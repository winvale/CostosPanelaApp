<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tesis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tesis">
        <activity android:name=".Inicio"></activity>
        <activity android:name=".Historial" />
        <activity android:name=".Grafico" />
        <activity android:name=".Descripcion" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AnalisisEspecifico" />
        <activity android:name=".AnalisisGrafico" />
        <activity android:name=".AnalisisGeneral" />
        <activity android:name=".StartLoader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>